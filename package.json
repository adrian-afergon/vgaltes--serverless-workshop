{
  "name": "gettogether",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "cross-env SLSUSER=afergon serverless deploy --aws-profile serverless-local",
    "deploy:sid": "cross-env SLSUSER=afergon STAGE=sid serverless deploy --aws-profile serverless-local",
    "deploy:prod": "cross-env SLSUSER=afergon STAGE=prod serverless deploy --aws-profile serverless-local",
    "test:integration": "jest **/*.test.js",
    "test:acceptance": "cross-env TEST_MODE=http jest ./tests/*",
    "seed": "cross-env AWS_PROFILE=serverless-local node seedGetTogethers.js devafergon-gettogethers"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.618.0",
    "serverless": "^1.63.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.0",
    "jest": "^25.1.0",
    "serverless-iam-roles-per-function": "^2.0.2"
  }
}
